# テスト仕様書：ビジネスメール作成アプリ

## 1. テスト対象

- `app.py`（Flaskサーバー・APIバックエンド）
- `static/index.html`（フロントエンド画面）
- Ollamaを利用したビジネスメール生成処理

## 2. テスト環境

- OS：macOS、Windows 10／11
- Python 3.8以降
- Flask
- openaiライブラリ
- Ollama
- 使用モデル：`qwen2.5-coder:0.5b`
- ブラウザ：Google Chrome、Microsoft Edge、Safari

## 3. テスト項目

### 3.1 サーバー起動・静的ファイル配信

| No. | テスト内容 | 事前条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| 1 | Flaskサーバー起動 | 必要なライブラリがインストールされている | ターミナルまたはコマンドプロンプトで`python app.py`を実行する | エラーなくサーバーが起動する |
| 2 | トップ画面表示 | Flaskサーバーが起動している | ブラウザで`http://localhost:5000/`を開く | ビジネスメール作成画面が表示される |
| 3 | 静的ファイル配信 | Flaskサーバーが起動している | 画面を再読み込みする | レイアウトが崩れず、入力欄と実行ボタンが表示される |

※ポート5000が使用中の場合は、起動時に指定された別のURLへアクセスする。

### 3.2 `/send_api` API正常系

| No. | テスト内容 | 入力・操作手順 | 期待結果 |
|---|---|---|---|
| 4 | ビジネスメール生成 | インターンシップのお礼に必要な情報を入力して実行する | HTTP 200が返り、件名と本文を含むメールが生成される |
| 5 | 敬語への変換 | 簡単な言葉で内容を入力して実行する | 自然で失礼のない敬語に変換される |
| 6 | 追加条件の指定 | `context`に「簡潔に作成してください」と指定してAPIへ送信する | ビジネスメールのルールを維持しながら、追加条件が反映される |
| 7 | 複数行の入力 | 改行を含む文章を入力して実行する | エラーにならずメールが生成される |
| 8 | 記号を含む入力 | 句読点や記号を含む文章を入力して実行する | エラーにならずメールが生成される |

### 3.3 `/send_api` API異常系

| No. | テスト内容 | 入力・操作手順 | 期待結果 |
|---|---|---|---|
| 9 | 空文字の送信 | 入力欄を空にして実行する | HTTP 400と`Input text cannot be empty`が返る |
| 10 | 空白だけの送信 | スペースだけを入力して実行する | HTTP 400と`Input text cannot be empty`が返る |
| 11 | `text`フィールドなし | `text`を含まないJSONをAPIへ送信する | HTTP 400と`Missing 'text' in request body`が返る |
| 12 | JSONデータなし | JSONを送信せずにAPIを実行する | HTTP 400のエラーが返る |
| 13 | Ollama未起動 | Ollamaを終了した状態で実行する | HTTP 500と`AIサービスとの通信中にエラーが発生しました。`が返る |
| 14 | AIから有効な応答がない | AIの応答内容が空の場合 | `AIから有効な応答がありませんでした。`が返る |

### 3.4 生成内容の確認

| No. | テスト内容 | 入力・操作手順 | 期待結果 |
|---|---|---|---|
| 15 | 件名の生成 | インターンシップのお礼に関する情報を入力する | 入力内容に合った件名が生成される |
| 16 | 本文の生成 | インターンシップのお礼に関する情報を入力する | 挨拶、お礼、具体的な感想、結びを含む本文が生成される |
| 17 | 未入力情報の追加防止 | 担当者名や日付を入力せずに実行する | 入力していない担当者名や日付を勝手に追加しない |
| 18 | 入力内容の保持 | 「社員との座談会が印象に残った」と入力する | 入力した内容の意味を変えずにメールへ反映する |
| 19 | 件名と本文の分離 | 必要な情報を入力して実行する | 件名と本文が区別できる形式で出力される |

### 3.5 UIテスト

| No. | テスト内容 | 操作手順 | 期待結果 |
|---|---|---|---|
| 20 | 入力内容の保持 | 入力欄に文章を入力する | 入力した文章が画面に表示される |
| 21 | 処理中表示 | 実行ボタンを押す | AIの応答が返るまで`処理中...`と表示される |
| 22 | 正常結果表示 | メール生成を実行する | 結果欄に生成されたメールが表示される |
| 23 | エラー表示 | Ollamaを停止して実行する | 結果欄に`エラー: ...`と表示される |
| 24 | 再実行 | 入力を変更してもう一度実行する | 新しい入力内容に対応した結果へ更新される |

### 3.6 境界値・その他のテスト

| No. | テスト内容 | 入力・操作手順 | 期待結果 |
|---|---|---|---|
| 25 | 短い入力 | 1文字だけ入力して実行する | サーバーが停止せず、結果または適切なメッセージが返る |
| 26 | 長い入力 | 長い文章を入力して実行する | サーバーが停止せず、結果または適切なエラーが返る |
| 27 | 日本語以外の入力 | 英語を入力して実行する | サーバーが停止せず応答が表示される |
| 28 | 応答時間 | 実行ボタンを押してから結果表示までを計測する | 応答にかかった時間を記録できる |
| 29 | OSの違い | macOSとWindowsの両方で起動・実行する | どちらの環境でも画面表示とメール生成ができる |

## 4. 合格条件

- サーバーが正常に起動すること
- 正常な入力に対して件名と本文が生成されること
- 空入力などの不正なリクエストに適切なエラーが返ること
- Ollamaとの通信に失敗してもFlaskサーバーが停止しないこと
- macOSとWindowsの両方で主要機能が動作すること